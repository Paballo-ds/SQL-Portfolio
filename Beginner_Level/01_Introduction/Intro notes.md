INTRO TO SQL

WHY USE A DATABASE?
- Storing all data 
- Can store large amounts of data
- SQL (Data Query Language)
  allows you to talk to the database,
  you can retrieve information
- secure, you can control who gets access to what

WHAT IS DBMS & SQL SERVER?
DATABASE MANAGEMENT SYSTEM
- DBMS is a software that can manage the database
  all the requests coming from people,apps and tools.
  Accomplishing them through prioritising
- Manages the security, whether or not the request is secure
  SERVER
  - where the DataBase lives
 
  TYPES OF  DATABASES:
SQL DATABASE
 * 1) Relational (Microsoft SQL Server, MySQL, PostGre)
NO SQL DATABASE
  2)Key-Value 
  3)Column-Based
  4)Graph
  5)Document

DATABASE STRUCTURE HIERARCHY:
1) SERVER
2) can host multiple DATABASES
3) can have mutiple SCHEMA grouping related objects
4) can have multiple TABLES organising and storing your data into different categories

TYPES OF SQL COMMANDS
1) DDL = Data Definition Language
   CREATE, ALTER, DROP
2) DML = Data Manipulation Language
   INSERT, UPDATE, DELETE
3) SQL = Data Query Language
   SELECT

there is data in tables, you query the database thats storing the tables and it gived you the results

WHY LEARN SQL?
-talk to the data
-high demand
-industry standard
